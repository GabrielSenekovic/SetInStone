using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Balloon
{
    /* bool pop = false;

    private void FixedUpdate() 
    {
        if(pop && gameObject.GetComponent<Renderer>().material.GetFloat("_PopLevel") >= 0)
        {
            var objRenderer = gameObject.GetComponent<Renderer>();
            objRenderer.material.SetFloat("_PopLevel", objRenderer.material.GetFloat("_PopLevel") + 0.1f);
        }
    }
    public override void OnBeAttacked(int value)
    {
        pop = true;
    } */
}
